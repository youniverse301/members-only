extends layout

block content
  h1= "header"
  if user
    a(href="/new-message") New Message 
    if user.status !== "member" && user.status !== "admin"
      a(href="/become-member") Become Member 
    if user.status !== "admin"
      a(href="/become-admin") Admin Mode 
    a(href="/log-out") Log Out 
  else
    a(href="/sign-up") Sign Up 
    a(href="/log-in") Log In

  h1= title
  p Welcome to #{title}

  if messages.length
    if user
      each message in messages
        div
          h3= message.title
          em= "by " + message.author.first_name
          p= message.text
          p= message.timestamp
    else
      each message in messages
        div
          h3= message.title
          p= message.text
